import React from 'react'

const Toolbar = () => {
  return (
    <div>
      <input type="color" />
      <button>Undo</button>
      <button>Redo</button>
      <select>
        <option>Option 2</option>
      </select>
    </div>
  )
}

export default Toolbar
